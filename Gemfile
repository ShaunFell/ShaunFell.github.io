source "https://rubygems.org"

# The github-pages gem pins the exact Jekyll version and plugins that GitHub
# Pages uses for "deploy from branch" builds, so local builds and CI match
# the live site byte for byte.
gem "github-pages", group: :jekyll_plugins
gem "webrick"
